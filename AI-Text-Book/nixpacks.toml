providers = ["python"]

[phases.setup]
nixPkgs = ["python312", "gcc", "libffi", "openssl"]

[phases.install]
cmds = ["pip install -r api/requirements.txt"]

[start]
cmd = "cd api && uvicorn src.main:app --host 0.0.0.0 --port $PORT"
